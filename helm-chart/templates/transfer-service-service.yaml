{{- $svc := index .Values "transfer-service" }}
{{- if $svc.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "banking.transfer-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "banking.transfer-service.fullname" . }}
    {{- include "banking.transfer-service.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "banking.transfer-service.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ $svc.service.port }}
      targetPort: {{ $svc.service.port }}
      name: {{ $svc.service.portName }}
{{- end }}

