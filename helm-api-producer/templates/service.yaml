apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-producer.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "api-producer.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "api-producer.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
